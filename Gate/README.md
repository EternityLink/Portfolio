This is a website for a student game I helped make. The website was created for the purpose of learning html, css and javascript.

The embedded Youtube video only seems to work when the html is hosted on a server (tested with localhost), not when running directly from a file directory.
